-- Server entry point
-- All server-side scripts are children of this module in ServerScriptService.
-- Each system (GameManager, ShipSystem, CombatSystem, etc.) will be a sibling
-- module that the server bootstrap requires and initialises.

return {}
