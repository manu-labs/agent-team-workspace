-- Client entry point
-- All client-side scripts are children of this module in StarterPlayerScripts.
-- Each controller (ShipController, CameraController, etc.) will be a sibling
-- module that the client bootstrap requires and initialises.

return {}
