-- Shared modules entry point
-- All modules here land in ReplicatedStorage so both server and client can
-- require them.  Shared modules must be stateless — data definitions,
-- configuration tables, type aliases, and pure utility functions only.

return {}
