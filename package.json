{
  "name": "agent-team-workspace",
  "private": true,
  "scripts": {
    "build": "cd ${APP_DIR:-stock-analysis-dashboard/backend} && npm install",
    "start": "cd ${APP_DIR:-stock-analysis-dashboard/backend} && node server.js"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
