# StockPulse Backend Configuration

# Server
PORT=3001
NODE_ENV=development

# CORS — set to your frontend URL in production
# Example: https://manu-labs.github.io
# Comma-separated for multiple origins
CORS_ORIGIN=*

# Groq AI
GROQ_API_KEY=your_groq_api_key_here

# Data Sources (optional — free tiers)
ALPHA_VANTAGE_API_KEY=
FINNHUB_API_KEY=
NEWS_API_KEY=

# Database
DB_PATH=./data/stockpulse.db

# Rate Limiting
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=100

# SEC EDGAR (required by EDGAR ToS — use a real contact email)
EDGAR_CONTACT_EMAIL=contact@yourdomain.com

# Earnings transcript storage limit (chars, for Groq context window)
# Default: 50000 (~12k tokens). Groq llama3 supports up to ~128k tokens.
TRANSCRIPT_MAX_CHARS=50000
